{
  "name": "eoex",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "optimize:mag-covers": "node scripts/optimize-mag-covers.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "live:local": "npm run build && vite preview --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "vite": "^8.1.0"
  }
}
